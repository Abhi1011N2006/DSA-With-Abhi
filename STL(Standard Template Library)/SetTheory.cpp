/*
SET : 1] store unique value same as in math set , internally it is tree
        2] store in sorted order
        3] need to include hidder file #include<set>


    creating :
        set<int> s;

    function :
        1]insert / emplace :
        2]count :
        3]erase :
        4]find :
        5]size , empty ,erase :

MULTISET : multiset<int> s;  -> contain duplicate element
UNORDERED SET : unordered_set<int> s; -> not store in sorted manner 
                complexity : O(1)
*/